const Project = ({props}) =>{
    return(
        <section className="section project">
            <img src={props.img} className="project__background"></img>
            <article className="project navigation">

            </article>
        </section>

    );
}

export default Project;